# Legacy Streamlit app (archival)

This folder is retained for archival/reference purposes only.

It is **not** the supported Rosettier v2 application path for current use or publication.
Use the maintained app entrypoint from `src/rosettier_app` via:

```bash
rosettier-app
```
